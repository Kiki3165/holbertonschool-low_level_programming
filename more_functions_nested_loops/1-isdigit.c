#include <ctype.h>
#include "main.h"
/**
 * _isdigit - nested loops
 *
 *@c: Number to pass to _isdigit function
 *
 * Return: 0
 */
int _isdigit(int c)
{
int c = n % 10
if (c < 0)
c = c * -1;
_putchar(c + '0');
return (c);
}
