#include <stdio.h>
#include <ctype.h>
#include "main.h"
/**
 * _isdigit - nested loops
 *
 *@c: Number to pass to _isdigit function
 *
 * Return: 0
 */
int _isdigit(int c)
{
int i;
for (i = 0; i <= 9; i++)
{
if (i = n % 10)
{
return (0);
else
return (1);
}
}
}
