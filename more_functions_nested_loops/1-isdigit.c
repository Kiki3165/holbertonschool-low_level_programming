#include <ctype.h>
#include "main.h"
/**
 * _isdigit - nested loops
 *
 *@c: Number to pass to _isdigit function
 *
 * Return: 0
 */
int _isdigit(int c)
{
int c;
for (c = 0; c <= 9; c++)
{
if (c = n % 10)
return (0);
else
return (1);
}
}
