#include <stdio.h>
#include "main.h"
/**
 * mul - check the code
 *
 *@a: number multiplicate
 *@b: number multiplicate to the first
 * Return: Always 0.
 */
int mul(int a, int b)
{
return (a * b);
}
