#include "main.h"
#include <stdio.h>

/**
 * _strcat - check the code
 *
 *@dest: concatenate
 *
 *@src: concatenate to
 *
 * Return: Always 0.
 */

char *_strcat(char *dest, char *src)
{
char dest[];
char src[];
_strcat(src,dest);
_putchar('\n');
return (dest + src);
}
