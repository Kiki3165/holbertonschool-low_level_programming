#include "main.h"
#include <stdio.h>

/**
 * string_toupper - check the code
 *
 *
 *
 * Return: Always 0.
 */

char *string_toupper(char *)
{
int i;
for (i = 0; * != '\0'; i++)
{
if (* >= 'a' && * <= 'z')
*[i] =(int)*[i] - 32;
}
return (*);
}
