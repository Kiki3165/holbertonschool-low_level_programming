#include <stdio.h>
/**
 * main - check if a number
 *
 * Return: 0
 */
int main(void)
{
int num;
for (num = 0; num < 16; num++)
putchar((num % x 16) + '0');
for (num = 'a'; num <= 'f'; num++)
putchar('\n');
return (0);
}
