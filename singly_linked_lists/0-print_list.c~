#include <stdlib.h>
#include <string.h>
#include <stdio.h>
#include "lists.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */

size_t print_list(const list_t *h)
{
  int *temp
  temp = h;
  while (temp != NULL)
    {
      printf("%d ", temp->data);
      temp = temp->next;
}
